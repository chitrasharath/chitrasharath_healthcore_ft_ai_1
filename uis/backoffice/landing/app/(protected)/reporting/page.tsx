import { ReportingDashboard } from "@backoffice/reporting/components/reporting-dashboard";

/** Reporting hub — summary + KPI tabs + pipeline health. */
export default function ReportingPage() {
  return <ReportingDashboard />;
}
