# Backoffice feature module

**Run via the landing app only** (`uis/backoffice/landing/` on port **3001**).

This folder provides UI components imported by landing routes at `/backoffice-functions`. The standalone Next.js `app/` shell here is deprecated.
