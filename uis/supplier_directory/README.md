# Supplier Directory feature module

**Run via the landing app only** (`uis/backoffice/landing/` on port **3001**).

Routes: `/supplier-directory`, `/supplier-directory/suppliers/[id]`. The standalone Next.js app on port 3003 is deprecated.
