# Incident Analyzer feature module

**Run via the landing app only** (`uis/backoffice/landing/` on port **3004**).

Route: `/incident-analyzer`. The standalone Next.js app on port 3002 is deprecated.
